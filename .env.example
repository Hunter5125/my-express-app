# Environment Variables Template

# MongoDB Connection (Required for production)
# For local development:
# MONGO_URI=mongodb://127.0.0.1:27017/dayoff
# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dayoff?retryWrites=true&w=majority

MONGO_URI=

# Session Secret (Change this to a random string)
SESSION_SECRET=your-secret-key-change-this

# Server Port (Usually handled by hosting platform)
PORT=3000

# Node Environment
NODE_ENV=production
