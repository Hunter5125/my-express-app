<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - DayOff System</title>
    <link rel="stylesheet" href="/javascripts/login.css">
</head>
<body>
    <article class="auth-card">
        <h2>Sign in</h2>
        <form action="/login" method="post" novalidate>
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="Enter your email" required autocomplete="email">
            <label for="password">Password</label>
            <input id="password" name="password" type="password" placeholder="Enter your password" required autocomplete="current-password">
            <button type="submit" class="btn">Sign in</button>
        </form>
        <p class="muted">Don't have an account? <a href="/register">Register</a></p>
        {{#if error}}
            <div class="error">{{error}}</div>
        {{/if}}
    </article>
</body>
</html>